interface:
    display_name: 'Determine Optimality'
    short_description: 'Evaluate whether a subject is truly optimal'
    default_prompt: 'Use $determine-optimality to evaluate whether this subject is truly optimal.'

policy:
    allow_implicit_invocation: true

dependencies:
    tools:
        - type: 'mcp'
          value: 'context7'
          description: 'Context7 MCP server for current technology documentation used in optimality evaluation'
